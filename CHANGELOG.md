# Changelog

## Unreleased
- Removed embedded OpenClaw plugin and browser/CDP components. The repo now contains only the Worker.

## 0.1.0
- Initial release: Worker (D1 + Vectorize + CDP)
- OpenClaw memory plugin (memory-d1)
- Cloudflare browser skill
